# Bridge `0.3.1` 双向实时 CLI 候选记录

日期：2026-07-23

执行人：Codex（实现、Linux x64 自动化与本地附着）；用户（真实窗口重载）

复核人：待补测

## 变更与触发项

| 项目 | 内容 |
| --- | --- |
| Bridge 版本 | `0.3.1` |
| 基线提交 | `90dd9a4`；`0.3.0` 与本功能候选尚未提交 |
| 变更摘要 | 本地 CLI 通过受鉴权远程 TUI 恢复普通本地或 Remote SSH 窗口的 VS Code 同一 thread；POSIX 普通 `codex` 在重启后自动接入，双向消费实时事件 |
| 命中的升级触发项 | app-server transport、CLI 接入、Shim、权限继承、生命周期、VS Code/Remote SSH 真实链路 |
| 声明范围 | Linux x64 自动化与普通本地窗口双向同 thread 通过；Remote SSH 回归和 Windows x64 待补测 |

## 兼容集合

| 组件 | 版本 | 策略或状态 |
| --- | --- | --- |
| VS Code | `1.130.0` | 本次实测值 |
| 官方 Codex 扩展 | `26.715.61943` | 不固定版本；普通本地窗口真实重载通过 |
| Bridge Controller | `0.3.1` 候选 | 单目标第三段版本升级 |
| Remote Executor / 协议 | `0.2.7` / v4 | 未改变 |
| 官方扩展内置 Codex/app-server | `0.145.0-alpha.27` | 唯一 app-server 来源 |
| 系统 Codex CLI | 本次探针 `0.145.0`，不固定 | 仅作为远程客户端；运行时探测 `--remote` |

## 门禁

| 门禁 | 状态 | 证据或阻塞原因 |
| --- | --- | --- |
| 版本、类型、测试、构建、审计 | 通过（自动化） | `npm run check`：37 文件通过、1 文件条件跳过；155 项通过、5 项跳过；`npm audit` 为 0 |
| 普通本地窗口共享入口 | 通过 | 真实窗口生成 `host=local` 描述符；`codex-vscode` 恢复同一 thread 并显示当前对话、流式工具事件和历史 |
| CLI 集成自动协调 | 通过 | 重载后审计记录 MCP `unchanged`、启动器 `updated`；无需执行启用命令 |
| 普通 `codex` 自动接管 | 普通本地实机通过 | 重载后自动保存官方绝对入口与原始相对链接并接管；`--version` 正常透传；无参数启动恢复相同 thread，实时显示当前输入、回复、工具过程和历史；进程、loopback 与连接/断开审计通过 |
| 同 thread 双向输入与流式事件 | 普通本地实机通过 | 修复前只证明 VS Code→CLI；修复后外部连接启动真实 turn，收到 7 段 `item/agentMessage/delta` 和 `BRIDGE_LOCAL_STREAM_OK_2`，Bridge 向 VS Code 主 stdio 转发 10 条通知，官方 Codex 日志确认收到并绑定该 turn；普通 `codex` 随后显示同一历史 |
| 双向取消与一端断开 | 普通本地实机通过 | CLI 以 Escape 取消活动 turn 后收到 interrupted，官方 UI 收到 thread 状态变化；三次附着均正常断开 |
| 令牌不进入 argv/日志/配置 | 通过 | 真实进程参数只含 endpoint、环境变量名和 thread ID；审计只记录随机 client ID |
| Linux x64 Controller 包 | 通过 | 当前平台 VSIX 已构造并校验 |
| Windows x64 | 待补测 | Linux 环境不替代 Windows 构造和实机 |

## 产物

| 文件 | 字节数 | SHA-256 | 状态 |
| --- | ---: | --- | --- |
| `codex-remote-bridge-0.3.1-win32-x64.vsix` | 待补测 | 待补测 | 本次未构造 |
| `codex-remote-bridge-0.3.1-linux-x64.vsix` | 849024 | `b835291763bd4524164abf365e906c7e7c4be3a589460e58ac8c604a0ca95e9d` | 通过 |
| `codex-remote-bridge-executor-0.2.7-linux-x64.vsix` | 40875 | `af378b86ab96ee930f859b8aa92b56237c1eafb8e58aa88d9c532ff5223d2a9a` | 通过 |
| `codex-remote-bridge-executor.vsix` | 40875 | `af378b86ab96ee930f859b8aa92b56237c1eafb8e58aa88d9c532ff5223d2a9a` | 与版本化 Executor 一致 |

## 当前结论

`0.3.0` 的 MCP 控制面不是实时镜像。`0.3.1` 已完成普通本地窗口显式入口的同 thread
双向输入、事件和取消闭环，并补上普通 `codex` 下一次启动自动接入的安全启动链。此次
复核修复了一个此前被假服务器广播行为掩盖的缺口：每个外部 CLI 拥有独立上游连接，
官方 app-server 不保证把它的通知送到 VS Code 上游；现在由 Bridge 广播无 ID 通知并
去重，响应和服务端请求仍保持原连接路由。扩展安装状态变化会触发自动协调，Shim 迁移
后自动重载窗口。
当前已运行的旧 CLI 无法热切换；普通入口安装后本地实测已通过。权限、Remote SSH
回归和 Windows x64 闭环通过前仍只表示实施候选，不替代 `0.2.7` 已验收基线。
