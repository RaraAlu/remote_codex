# Bridge `0.3.73` 拖放权限自动引导验收

日期：2026-08-10

执行人：用户（权限确认、窗口重载、本地与 Remote SSH 实机验证）、Codex（实现、日志、
哈希、自动化和产物复核）

## 范围

本记录覆盖 Linux x64 Controller 的原生拖放权限自动引导、polkit 写入、成功后自动重载、
同一资产组合去重、无焦点欢迎页降级和 Remote SSH 窗口回归。Windows x64、兼容层禁用后
的逐字节恢复以及未来 VS Code/官方扩展升级后的重打补丁保持待补测。

## 组件

| 组件 | 实测值 |
| --- | --- |
| Bridge Controller / Shim | `0.3.73` |
| Remote Executor | `0.2.21` |
| VS Code | `1.132.0` Linux x64 |
| 官方 Codex 扩展 | `openai.chatgpt@26.803.61601` Linux x64 |
| Remote SSH 目标 | `test_40`，Linux x64，活动 VS Code transport |

版本仅作为本次诊断证据，不作为运行时准入条件。

## 实机结果

1. 用户先通过禁用命令恢复旧补丁，再加载 `0.3.73`。Bridge 于 `16:36:17` 自动请求一次
   用户同意和所需文件访问；用户完成 polkit 后，官方 Webview 于 `16:36:18`、Workbench
   与 `product.json` 于 `16:36:21` 分别记录 `patched`。
2. 两组补丁均成功后，Bridge 于 `16:36:22` 调用 `reloadWindow`。新 Extension Host 于
   `16:36:23` 激活，官方 app-server 于 `16:36:26` 完成 `initialize`；同一资产指纹没有
   再次显示权限确认。
3. `/usr/share/code` 下 Workbench 与 `product.json` 的实际 SHA-256 分别为
   `754cd566755afd3d6d7475ede90fe16e6854fd0cd0aa70caa28b4f7d48807d3e` 和
   `36e6a98a977812d8465b1382439b1fa62ea6fd3b6cbd52b7b8c883e04d99e46e`；官方 Webview 为
   `e2e0cab24d3a8e69f579cbf353d8222ceb75a0a176b555b72d9e67dda68104b7`。三者均与托管补丁
   元数据一致。
4. 欢迎页没有可用 `focusedView` 时，Bridge 不再调用会弹出错误框的布局重置；用户重载
   后确认“当前没有重点视图”不再出现。当前 VS Code 未公开焦点查询命令，最终候选直接
   记录 `unavailable` 并保持布局不变。
5. Remote SSH 窗口加载 `0.3.73` 后，Controller 于 `16:48:12` 从 `degraded` 到达
   `ready`，日志确认配套 Executor `0.2.21`；随后目录拖放以
   `source="system-file-manager"`、`insertionMode="inline-mention"` 完成，附加数为 1、
   失败数为 0。用户确认远程窗口行为正常。

## 自动化与产物

| 项目 | 结果 |
| --- | --- |
| `npm run check` | 通过：410 项通过、7 项按预期跳过，类型检查、构建、Shim 冒烟与构包均通过 |
| Linux x64 Controller VSIX | `44,710,389` 字节；SHA-256 `0970e3f6f4e84f3abb29a3839665167e018b18003848974750af8ab4f193b121` |
| Remote Executor VSIX | `0.2.21`；`70,317` 字节；SHA-256 `10415e6f5729fbc6e88f55bb8fd3def024c2e783394fe05916a4b2667c209019`，版本化与内嵌副本一致 |
| `npm audit --audit-level=high` | 通过：0 个漏洞 |

## 门禁结论

| 门禁 | 状态 | 说明 |
| --- | --- | --- |
| G1 自动化 | 通过 | 最终完整检查通过 |
| G2 产物 | 通过（Linux） | Linux 产物身份已记录；Windows 仍待原生平台构建与验证 |
| G3 官方界面 | 通过（Linux） | 自动重载后 app-server `initialize` 成功 |
| G4 Remote SSH | 通过（本目标） | 到达 `ready`，目录拖放成功 |
| G6 生命周期 | 通过（本目标） | 权限确认、补丁、自动重载和重载后恢复成功 |
| G7 安全失败 | 通过（本目标） | 无静默提权；哈希一致；未知焦点能力失败关闭 |
| G8 双平台 | 待补测 | Windows x64 未执行 |

最终声明仅覆盖 Linux x64 上本目标的自动引导和重载链路，不扩大为完整双平台发布通过。
