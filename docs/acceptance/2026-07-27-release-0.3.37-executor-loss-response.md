# Bridge `0.3.37` Executor 独立失联写入验收记录

日期：2026-07-27

执行人：用户（`g1_1` Remote SSH 窗口重载）；Codex（问题复现、实现、自动化、
故障注入、日志、审计、远端后置检查和产物复核）

复核人：待补测（当前候选官方 UI、冷/热启动最低样本、最终窗口关闭及 Windows x64）

## 变更与触发项

| 项目 | 内容 |
| --- | --- |
| Bridge 版本 | `0.3.37` |
| 基线提交 | `0eaa753` |
| 变更摘要 | Executor 独立失联时把已发送副作用收敛为不可重试 `RESULT_UNKNOWN`；写入替换前校验精确 stdin 字节数；清理死亡 Extension Host 遗留的写入登记和临时文件 |
| 命中的升级触发项 | Bridge Controller/Shim、Remote Executor、诊断协议、VS Code Remote transport、断线恢复、安全写入与生命周期 |
| 声明范围 | Linux x64 候选在 `g1_1` VS Code Remote SSH 的 Executor 独立失联写入完整性 |

## 问题与修复

精确 `0.3.34` 的独立失联探针在远端 `.codex-bridge-write.*` 出现后终止 Extension
Host。新 Extension Host 能自动启动，但旧写入把短 stdin 当作正常 EOF，以部分新内容
替换目标；调用方同时收到可重试 `REMOTE_TRANSPORT_DISCONNECTED`，存在错误重试风险。

修复分三层完成：

- 已发送副作用收到 `REMOTE_TRANSPORT_DISCONNECTED` 或 `SSH_DISCONNECTED` 响应时，
  Controller 提升为 `RESULT_UNKNOWN`，使用原幂等键查询结果，不自动重放；
- 远端原子写脚本接收声明字节数，只有临时文件大小精确匹配才允许替换目标；
- 写入在创建工作区临时文件前，先在 `/tmp` 创建包含规范工作区根和临时路径的受限
  登记。新 Executor 激活时只检查当前用户、死亡拥有 PID、当前打开工作区和严格匹配
  文件名的登记，清理对应临时文件后删除登记。

Executor 能力集合新增 `executeStdinExactLength` 和
`workspaceWriteOrphanCleanup`，诊断协议更新为 11。Controller 依据实际能力决定部署，
不使用 Executor 包版本或协议号作为运行时门禁。

## 兼容集合

| 组件 | 本次实测值 | 证据 |
| --- | --- | --- |
| VS Code | `1.130.0` / Linux x64 | 本机 `code --version` 和活动 Remote SSH 窗口 |
| Remote SSH | `0.124.0` | 本机扩展清单 |
| 官方 Codex 扩展 | `openai.chatgpt@26.721.41059` | 活动插件与会话元数据 |
| Bridge Controller/Shim | `0.3.37` / `0.3.37-efb8ea7d5b649882` | 已安装扩展和活动 Shim 路径 |
| Remote Executor / 协议 | `0.2.19` / 11 | 远端扩展清单、能力驱动升级和真实 transport 调用 |
| 官方内置 Codex/app-server | `0.146.0-alpha.3.1` | 当前会话元数据 |
| Remote SSH 目标 | `g1_1` / `/home/unitree/mimiclite-sim2real` | 当前会话配置和 Bridge 审计 |

全部探针复用已打开的 VS Code Remote SSH transport，没有启动 OpenSSH 回退或第二次
认证。Windows x64 暂停，不从 Linux 结果推断。

## 自动化

- 定向测试覆盖 transport 错误响应提升、原幂等键恢复、精确 stdin 长度、短流不替换、
  能力缺失部署和死亡拥有者清理；6 个文件、37 项测试通过。
- 新增真实本地 shell 写入脚本测试，声明长度大于 stdin 时返回
  `PROTOCOL_MISMATCH`，目标不存在且临时文件为 0。
- 新增登记清理测试：死亡拥有者且属于当前根的临时文件和登记被删除；存活拥有者和
  根外记录保持不变。
- `npm run check` 退出 0：60 个测试文件通过、1 个真实远端条件文件跳过；267 项
  通过、6 项跳过、0 失败；Vitest 耗时 3.59 秒。类型检查、构建、Shim smoke 和 Linux
  当前平台构包全部通过。

## Remote SSH 实机

用户安装并重载精确 `0.3.37` 后，活动 app-server 使用
`0.3.37-efb8ea7d5b649882/codex-bridge-shim.cjs`。远端 Executor 自动升级为
`0.2.19`；远端 `extension.cjs` 与本地内嵌实现 SHA-256 均为
`1bc1e6d4f04c96631484708df6ca29434523a0069a6b71fbcd022befa76814bd`。
审计记录首轮 `09:27:39.310Z` 进入 `configuring`、`09:27:43.664Z` 到达
`ready`，热启动为 4,354 ms；第二轮从 `09:43:27.568Z` 到 `09:43:31.567Z`，
热启动为 3,999 ms；第三轮从 `09:46:37.347Z` 到 `09:46:41.218Z`，热启动为
3,871 ms。三轮 P50 为 3,999 ms、最大值为 4,354 ms、失败数为 0。第三轮活动 Shim
仍来自相同内容地址，远端 Executor 摘要不变；
预检确认目标文件不存在，工作区临时文件、当前用户登记和观察进程均为 0。

第一次冷启动由用户关闭 Remote SSH 窗口并重新打开同一工作区。审计从
`09:48:37.613Z` 的 `configuring` 到 `09:48:41.882Z` 的 `ready`，耗时 4,269 ms；
旧 Shim PID `1420089` 和旧会话描述符均不存在。新 Shim 使用同一精确内容地址，
Executor 为 `0.2.19`，目标、临时文件、登记和观察进程均无残留。
第二次冷启动从 `09:49:29.200Z` 到 `09:49:33.210Z`，耗时 4,010 ms；旧 Shim PID
`1423427` 和旧会话描述符同样已清理，新运行时身份与后置检查保持不变。
第三次有效冷启动从 `09:52:36.033Z` 到 `09:52:40.429Z`，耗时 4,396 ms；旧 Shim
PID `1425979` 和旧会话描述符已清理。三轮 P50 为 4,269 ms、最大值为 4,396 ms、
失败数为 0。用户第一次报告第三轮时，审计、PID 和会话描述符均未变化，因此未将该次
报告计入样本；完整关闭原窗口并重新打开后才记录上述第三个有效样本。

最终故障注入先创建 28 字节原文件
`ORIGINAL_EXECUTOR_LOSS_0337\n`，SHA-256 为
`56d0e286085879818c8ab51f8e13e93b86d0450f2fb5554bd7fb3e3d64b2d1da`。
观察进程在工作区临时文件出现后立即向 Extension Host 发送 `SIGKILL`：

| 检查 | 结果 |
| --- | --- |
| Extension Host PID | `52431 -> 56533`，自动恢复 |
| 调用结果 | `RESULT_UNKNOWN`，`retryable=false` |
| 结果查询 | 原幂等键，`lastStatus=unreachable`，`recoveryAttempts=3` |
| 目标文件 | 原 SHA-256 不变，大小 28 字节，原前缀不变 |
| 工作区临时文件 | 0 |
| `/tmp` 写入登记 | 0 |
| 写入观察进程 | 0 |
| 自动重放 | 0；审计只有同一请求的一次 `started` 和一次 `unknown` |

审计时间为 `09:31:23.870Z` 到 `09:31:47.306Z`。独立的新 Executor 后置探针再次
确认目标文件、临时文件、登记和观察进程均不存在，远端 Executor 仍为 `0.2.19`，
实现摘要不变。

## 产物

| 文件 | 字节数 | SHA-256 | 包内身份 | 状态 |
| --- | ---: | --- | --- | --- |
| `codex-remote-bridge-0.3.37-linux-x64.vsix` | 1,034,489 | `d8830be72120ca273d4d37b3c7ebbee51a765ed67c9dd28ec1f2b2cf1f94c1d3` | Controller `0.3.37`，仅 CJS Shim，内嵌 Executor `0.2.19` | 通过 |
| `codex-remote-bridge-executor-0.2.19-linux-x64.vsix` | 69,139 | `1ac6f882921fe1458e4c498dd9741fc9cd8e215722b99d1445d733fbe488a4fb` | Executor `0.2.19` / Linux workspace 扩展 | 通过 |
| `codex-remote-bridge-executor.vsix` | 69,139 | `1ac6f882921fe1458e4c498dd9741fc9cd8e215722b99d1445d733fbe488a4fb` | 与版本化 Executor 字节一致 | 通过 |
| `codex-remote-bridge-0.3.37-win32-x64.vsix` | 待补测 | 待补测 | Windows 原生 stage 暂停 | 待补测 |

最终 Linux Controller VSIX 已强制安装。活动 Shim 与最终构建的 SHA-256 均为
`efb8ea7d5b649882448aa541a3d7ccba9e6dd94f44d8a87c50423f755cd73ea8`；远端 Executor
与最终内嵌实现的 SHA-256 均为
`1bc1e6d4f04c96631484708df6ca29434523a0069a6b71fbcd022befa76814bd`。

## 门禁与指标

| 门禁 | 状态 | 证据或阻塞原因 |
| --- | --- | --- |
| G0 诊断与能力 | 通过 | 精确 Shim 与 Executor 启动；两个新增实际能力驱动部署，不使用版本门禁 |
| G1 自动化 | 待补测 | `npm run check` 零失败；Windows 暂停，`package:all` 未执行 |
| G2 产物 | 待补测 | Linux Controller/Executor 原生 stage 通过；Windows Controller 暂停 |
| G3 官方界面/App Server | 待补测 | 精确 Shim 已恢复；当前候选官方 UI 新建和恢复最低样本未完成 |
| G4 Remote SSH 执行 | 通过 | 真实写入故障注入、文件完整性、能力升级和后置清理通过 |
| G5 stdio MCP | 待补测 | 本目标未修改；最终候选固定调用留在 L07 |
| G6 生命周期 | 待补测 | Extension Host 故障恢复、热启动和冷启动各 3/3 通过；最终带载窗口关闭未完成 |
| G7 安全失败 | 通过（本目标） | 未知结果不重放，原文件不变，临时文件、登记和观察进程均为 0 |
| G8 双平台 | 待补测 | Linux 实机通过；Windows 明确保留未验证 |
| G9 证据闭环 | 待补测 | 本修复闭环；`0.4.0` 其余人工矩阵和最终量化未完成 |

| 指标 | 样本 | 当前值 | 状态 |
| --- | ---: | --- | --- |
| 自动化 | 1 | 60 文件通过、1 跳过；267 测试通过、6 跳过；失败 0 | 通过 |
| `0.3.37` 热启动到 `ready` | 3/3 | P50 3,999 ms；最大值 4,354 ms；失败 0 | 通过 |
| `0.3.37` 冷启动到 `ready` | 3/3 | P50 4,269 ms；最大值 4,396 ms；失败 0 | 通过 |
| Executor 独立失联写入 | 1 | PID 自动切换；`RESULT_UNKNOWN`；原文件不变；临时文件、登记、观察进程为 0 | 通过 |
| 自动重放 | 1 | 0；审计一次 started/unknown | 通过 |
| 额外认证 | 1 次重载会话 | 0 | 通过 |
| 当前候选官方任务、冷/热启动和最终窗口关闭 | 未达最低样本 | 待补测 | 待补测 |

## 结论

`0.3.37` 已在精确安装的 Linux x64 Controller、当前 Shim、自动升级的 Executor
`0.2.19` 和真实 `g1_1` VS Code Remote SSH transport 上关闭 Executor 独立失联写入
的数据完整性问题。调用方得到不可重试未知结果，原文件不被短流替换，死亡拥有者的
临时文件与登记由新 Executor 清理，原操作不重放。

该目标可以作为独立、已验证的 Linux 行为修复提交。它不是 `0.4.0` 发布通过声明；
README 中剩余官方 UI、启动最低样本、审批/取消、视觉、权限撤销、最终窗口关闭和
Windows x64 范围继续保持 `待补测`。
