# Bridge `0.3.33` 普通本地 `full-access` 验收记录

日期：2026-07-26

执行人：用户（普通本地与 `g1_1` Remote SSH 窗口重载）；Codex（实现、自动化、
注入任务、日志、审计、会话和产物复核）

复核人：待补测（当前候选官方 UI、冷/热启动最低样本及 L03-L07）

## 变更与触发项

| 项目 | 内容 |
| --- | --- |
| Bridge 版本 | `0.3.33` |
| 基线提交 | `990c07e` |
| 变更摘要 | 外部新对话的 `full-access` 改用协议支持的 `sandbox=danger-full-access`，不再把显示模式误作命名权限档案 |
| 命中的升级触发项 | Bridge Controller/Shim、app-server `thread/start`、安全策略与审批 |
| 声明范围 | Linux x64 候选的普通本地共享网关及 `g1_1` VS Code Remote SSH 外部入口 |

Remote Executor 的实现、能力和协议没有变化，因此保持 `0.2.16` / 协议 9。

## 问题与修复

`0.3.32` 的外部 MCP 在用户选择 `full-access` 时发送：

```json
{"approvalPolicy":"never","permissions":"full-access"}
```

当前协议把 `permissions` 定义为命名权限档案 ID。普通本地共享网关保持官方请求语义，
因此 app-server 在 `thread/start` 具体失败为：

```text
default_permissions requires a [permissions] table
```

修复后的请求为：

```json
{"approvalPolicy":"never","sandbox":"danger-full-access"}
```

该字段形状已经由当前 app-server 的真实 `thread/start` 和项目操作证明可用，不依赖
任何组件版本值。普通本地网关原样传递 sandbox；Remote SSH 网关删除本地 sandbox，
映射到 Bridge 的命名本地拒绝档案，并继续把远端项目能力放在动态工具和既有审批边界
中。因而本地获得真实完全访问，Remote SSH 仍不能回退到本地项目执行。

## 兼容集合

| 组件 | 本次实测值 | 证据 |
| --- | --- | --- |
| VS Code | `1.130.0` / Linux x64 | 本机 `code --version` 和 `code --status` |
| Remote SSH | `0.124.0` | 本机扩展清单 |
| 官方 Codex 扩展 | `openai.chatgpt@26.721.41059` | 本机扩展清单 |
| Bridge Controller/Shim | `0.3.33` / `0.3.33-975ae2fee49d09fa` | 已安装扩展、两个活动 Shim 路径 |
| Remote Executor / 协议 | `0.2.16` / 9 | 本目标未修改 Executor，当前 VS Code transport 继续可用 |
| 官方内置 Codex/app-server | `0.146.0-alpha.3.1` | 两个本次 rollout 的 session 元数据 |
| Remote SSH 目标 | `g1_1` / `/home/unitree/mimiclite-sim2real` | 当前 session 配置和 `bridge.connect` 审计 |

两条任务都复用已打开窗口。Remote SSH 仅有既有 VS Code 动态转发 SSH 进程，没有启动
OpenSSH 回退或第二次认证。Windows x64 暂停，不从本结果推断。

## 自动化与产物

- 定向回归：
  `npx vitest run test/vscode-conversation-client.test.ts test/rewrite.test.ts`，
  2 个文件通过、10 个测试通过、1 个条件测试跳过。
- `npm run typecheck` 通过。
- `npm run check` 退出 0：59 个测试文件通过、1 个条件文件跳过；262 个测试通过、
  6 个跳过，最终 Vitest 耗时 3.56 秒；构建、Shim smoke 和 Linux 当前平台构包均通过。
- `npm run package:stage` 验证 Linux 包内版本、平台 Shim 和嵌入 Executor 一致性，
  并生成受控 Linux stage。Windows 原生 stage 按本轮范围暂停。
- 最终 VSIX 已重新安装；包内 Shim、安装目录 Shim 和两次实机实际运行的隔离 Shim
  SHA-256 均为
  `975ae2fee49d09fa568cb556b1ff32bcb1d7d3d4c7cb2260e12b3517e2305e25`。

| 文件 | 字节数 | SHA-256 | 包内身份 | 状态 |
| --- | ---: | --- | --- | --- |
| `codex-remote-bridge-0.3.33-linux-x64.vsix` | 1,028,939 | `6c77e4cb106aafd0d90d84d12bc67943dec98e47dc22834ce0b0cc54a6203d34` | Controller `0.3.33`，仅 CJS Shim，内嵌 Executor | 通过 |
| `codex-remote-bridge-executor-0.2.16-linux-x64.vsix` | 66,005 | `ee51a1bcd768cf47a5a9a5a2641b6407da19aa5f229258d8221c2c348654e9bc` | Executor `0.2.16` / Linux workspace 扩展 | 通过 |
| `codex-remote-bridge-executor.vsix` | 66,005 | `ee51a1bcd768cf47a5a9a5a2641b6407da19aa5f229258d8221c2c348654e9bc` | 与版本化 Executor 字节一致 | 通过 |
| `codex-remote-bridge-0.3.33-win32-x64.vsix` | 待补测 | 待补测 | Windows 原生 stage 暂停 | 待补测 |

## 普通本地实机

用户重载普通本地窗口后，活动进程为当前
`0.3.33-975ae2fee49d09fa/codex-bridge-shim.cjs`，会话描述符为 PID `1336490`、
`host=local`、`workspaceRoot=/home/zkbot`。

外部入口以 `full-access` 新建 thread
`019fa2a9-d790-7c31-8628-1ac43a27936d`，turn
`019fa2a9-d7d1-71b0-8447-56621428f439` 在 12,696 ms 内完成。Codex Core 真实执行一次
本地命令，读取候选 `package.json` 并运行 Git 根查询，结果为：

```text
codex-vscode-remote-bridge
0.3.33
/home/zkbot/work/remote
```

最终回复为 `LOCAL_FULL_ACCESS_0333_OK`。rollout 的 turn context 明确记录：

- `approval_policy=never`
- `sandbox_policy.type=danger-full-access`
- `permission_profile.type=disabled`
- `cwd=/home/zkbot`
- `workspace_roots=["/home/zkbot"]`

这证明普通本地窗口没有继承 Remote SSH 的本地拒绝档案，也没有因无效命名档案在
`thread/start` 失败。

## Remote SSH 实机

用户重载 `g1_1` 后，Remote SSH Shim 同样来自当前 `0.3.33` 安装目录。`configuring`
到 `ready` 为 4,055 ms，当前会话复用
`g1_1:/home/unitree/mimiclite-sim2real` 的 VS Code transport。

最终成功样本为 thread `019fa2ab-3a42-7821-b46c-f79c897a7fb7`、turn
`019fa2ab-3a7e-7900-8630-ea6cfa5b4abf`，8,923 ms 完成并回复
`REMOTE_FULL_ACCESS_0333_OK`。任务只调用一次 `remote_exec`，确认规范远端 cwd 且
`package.json` 是非空普通文件：

```text
pwd=/home/unitree/mimiclite-sim2real
package_json=nonempty_regular_file
```

审计记录：

- `remote_exec.approval` 1 次成功，`automatic=true`、
  `permissionMode=full-access`；
- `remote_exec` 1 次成功，`target=remote`、`rootId=remote-primary`、
  `rootRole=primary`、`remoteCwd=/home/unitree/mimiclite-sim2real`；
- 远端执行 375 ms，退出码 0，本地项目执行为 0；
- 自动 IDE 选区只记录 URI、范围、大小和 SHA-256，正文不进入审计。

此前 thread `019fa2aa-ab13-7bc1-9c58-3ec1063b53fd` 已成功远端执行 `pwd` 和
`git rev-parse`，但因该主根当前不是 Git 仓库而按任务条件返回诊断结果；两次 RPC
本身均远端成功且自动审批。该样本不计入成功任务指标，也不是 transport 或权限失败。

Remote SSH rollout 仍显示本地控制进程使用只读 sandbox 和 Bridge 托管本地拒绝档案；
远端完全访问仅通过经过目标、根、路径、幂等和审计约束的动态工具生效。

## 门禁与指标

| 门禁 | 状态 | 证据或阻塞原因 |
| --- | --- | --- |
| G0 诊断与能力 | 通过 | 当前 app-server 真实接受 sandbox 形状；远端按真实映射和操作通过，不使用版本门禁 |
| G1 自动化 | 待补测 | `npm run check` 零失败；双原生 `package:all` 因 Windows 暂停尚未完成 |
| G2 产物 | 待补测 | Linux Controller 和两个 Executor 产物通过；Windows Controller 暂停 |
| G3 官方界面/App Server | 待补测 | 当前 Shim 的本地/远程外部新 thread 通过；`0.3.33` 官方 UI 新建和恢复未采样 |
| G4 Remote SSH 执行 | 通过 | 当前 Shim、规范远端根、自动完全访问审批和远端只读命令通过，本地项目执行为 0 |
| G5 stdio MCP | 待补测 | 本目标未修改；最终候选固定调用留在 L07 |
| G6 生命周期 | 待补测 | 本次热重载 1/3 通过；冷启动和最终带载关闭未完成 |
| G7 安全失败 | 待补测 | 本样本无本地回退且审计不含正文；Core 诱饵和最终敏感扫描未完成 |
| G8 双平台 | 待补测 | Linux 实机通过；Windows 明确保留未验证 |
| G9 证据闭环 | 待补测 | 本修复闭环；`0.4.0` 的 L03-L07 和最终量化仍未完成 |

| 指标 | 样本 | 当前值 | 状态 |
| --- | ---: | --- | --- |
| 自动化 | 1 | 59 文件通过、1 跳过；262 测试通过、6 跳过；失败 0 | 通过 |
| `0.3.33` 热启动到 `ready` | 1/3 | 4,055 ms | 待补测 |
| 普通本地外部 `full-access` | 1 | 12,696 ms，项目操作成功，审批提示 0 | 通过 |
| Remote SSH 外部 `full-access` | 1 | 8,923 ms，远端执行 1/1，自动审批 1，本地执行 0 | 通过 |
| 额外认证 | 1 次重载会话 | 0 | 通过 |
| 当前候选官方任务、冷启动、固定探针和生命周期 | 未达最低样本 | 待补测 | 待补测 |

## 结论

普通本地外部 `full-access` 的具体消息兼容问题已经由 `0.3.33` 修复，并在精确安装
候选的普通本地与 `g1_1` Remote SSH 两条真实链路闭环。该提交可以作为独立、已验证的
Linux 行为修复保留。它不是 `0.4.0` 发布通过声明；官方 UI、最低量化、故障矩阵、
最终清理和 Windows 仍按 README 代办继续。
