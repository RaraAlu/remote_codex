# Bridge `0.3.30` Linux 可注入矩阵记录

日期：2026-07-26

执行人：Codex（`g1_1` 注入探针、托管 CLI 附着、审计与安全复核）

复核人：待补测（官方 UI 方向、真实窗口关闭、本地次级根及其余纯人工矩阵）

## 范围与边界

本记录追加在
`docs/acceptance/2026-07-26-release-0.3.30-external-disconnect.md` 之后，不改变候选实现
或版本。目标仍为 Linux x64 Controller、Remote SSH 主机 `g1_1` 和规范工作区
`/home/unitree/mimiclite-sim2real`。

- 当前 Controller/Shim 为 `0.3.30`，Shim 内容目录为
  `0.3.30-8aedab7317ae1dac`；Executor 保持 `0.2.16` / 协议 9。
- 所有远端操作复用当前 VS Code Remote transport，没有第二条 SSH 连接、密码提示或
  OpenSSH 回退。
- 本轮只补可由外部 CLI、共享 app-server、Bridge MCP 和活动 transport 精确注入的
  样本。官方面板视觉、本地次级根授权和真实窗口关闭不由注入结果替代。

## 审批矩阵

### `on-request` 接受

- 外部 CLI thread `019fa21d-a5f2-7862-925d-02a07d07121b` 的 turn
  `019fa21d-a632-7362-9738-2d67d2efcf21` 收到一次命令审批请求，测试端明确返回
  `accept`。
- 审计记录 `automatic=false`、`decision=accept`；`remote_exec` 经
  `remote-primary` 在 72 ms 内成功，最终输出为 `BRIDGE_APPROVAL_ACCEPT_0330`。
- turn 为 `completed`，耗时 13,341 ms。该样本证明需审批模式没有被外部 CLI 自动
  提升为 `full-access`。

### 审批等待中取消

- 外部 CLI thread `019fa21d-da62-70d2-bafb-8d5ee6827772` 的 turn
  `019fa21d-daa3-7bb2-9b8d-be205ce9dc7f` 在命令审批请求尚未响应时发送
  `turn/interrupt`。
- `remote_tool.cancel` 记录 `cancelledCalls=1`；审批结果为
  `automatic=false`、`decision=cancelled`，工具调用立即返回 `CANCELLED`。
- `turn/interrupt` 44 ms 内确认，turn 为 `interrupted`，耗时 6,938 ms。120 秒命令
  的唯一进程标记数为 0，证明审批等待中取消没有启动远端进程。

以上是外部 CLI 方向的功能样本。官方 UI 方向的三次取消、审批接受和审批等待中取消仍
保留为人工补测，不能由本记录代替。

## 写入失败关闭

### 多替换部分失败

- 外部 CLI thread `019fa21f-c0ef-74f2-b993-b455dd43d113` 的 turn
  `019fa21f-c12d-7443-b4b4-6f32867388ec` 创建 11 字节隔离文件，初始 SHA-256 为
  `e49c81e2d2f84e259d40e2fb8192f3bcd198b355184845d76d8f58807d0d78ee`。
- `workspace_apply_patch` 同时请求一个可命中的替换和一个不存在的源文本；调用在
  121 ms 后以 `FILE_CONFLICT` 失败。
- 失败后重新读取的 SHA-256 与正文均未改变，证明没有提交第一个替换，也没有产生部分
  写入。文件和空目录随后按原哈希删除。
- 创建、写入、失败补丁、复读和清理的审计均为 `clientSource=external-cli`、
  `target=remote`、`rootId=remote-primary`；变更审批为自动 `full-access`。

### 1 MiB 补丁上限

- 外部 CLI thread `019fa222-9f18-70d3-bc69-b08cb973ebd0` 的 turn
  `019fa222-9f57-7791-932b-0975f8988416` 创建 1,048,577 字节隔离文件，SHA-256 为
  `2cb74edba754a81d121c9db6833704a8e7d417e5b13d1a19f4a52f007d644264`。
- `workspace_apply_patch` 在 196 ms 后返回 `OUTPUT_TRUNCATED`，错误细节明确为
  `limitBytes=1048576`、`size=1048577`。
- 失败后重新读取的大小和 SHA-256 均未改变；文件和空目录随后清理。两次最终检查均
  确认 `.codex-bridge-acceptance-0330-*` 路径数为 0。
- 该样本验证远端补丁上限和失败原子性；`workspace_write_file` 超过 1 MiB 的原始
  Base64 请求仍由自动化覆盖，没有在实机对话中传输 1.4 MiB 工具参数。

### 补丁执行中外部客户端断开

- 外部 CLI thread `019fa227-96fa-7182-afae-6db31f3f6889` 的 turn
  `019fa227-973d-70e3-8397-b8bc67cdeaad` 对 1,048,576 字节隔离文件请求将开头的
  `HEAD` 替换为 `TAIL`。原文件 SHA-256 为
  `ddaea4d038229aba2aa057f2c61285b5b68873857321fd79a0fabbc8a6d0d5cb`，若完整提交则
  应为 `f0808b2f0003d3715238c69f8aef914c3781ee5df3b9c2257a7493350f765757`。
- 仅在审计出现该 `workspace_apply_patch` 的 `started` 后断开外部 WebSocket。45 ms
  后断开清理完成，turn 中断请求为 1、确认为 1、未确认为 0；随后通过当前 VS Code
  transport 独立读取，文件仍为原 SHA-256、原大小和 `HEAD` 前缀，临时文件数为 0。
- Bridge MCP 复读到 turn 最终为 `interrupted`，耗时 10,917 ms；隔离文件和目录随后
  清理。因调用方 transport 已关闭，工具审计只保留可信的 `started`，没有伪造成功或
  失败终态；断开后的文件完整性由独立 transport 复核。

## Core 本地阻断

独立外部 CLI 客户端直接提交四种本地 Core 风险请求：

| 方法 | JSON-RPC 结果 | 审计 `knownMethod` | 审计结果 |
| --- | ---: | --- | --- |
| `fs/readFile` | `-32003` | `true` | `failed` |
| `command/exec` | `-32003` | `true` | `failed` |
| `thread/shellCommand` | `-32003` | `true` | `failed` |
| `fs/futureDangerousOperation` | `-32003` | `false` | `failed` |

- 未知但属于 `fs/` 风险命名空间的方法同样失败关闭，没有依赖固定方法表放行。
- 从本次 `0.3.30` 重载到扫描时，审计中的成功 `target=local` 项目操作数为 0。
- 动态远端工具仍正常到达 `remote-primary`，证明本地 Core 阻断没有破坏专用远端路径。

官方 UI 附件、本地同名诱饵和普通本地窗口透传仍需要人工视觉和窗口上下文，继续保持
`待补测`。

## 远端 Git 工具

- 因目标主根原本不是 Git 仓库，先以严格的“不存在 `.git`”前置条件建立隔离 Git
  元数据目录，并用根目录 `.git` 指针临时关联；exclude 规则屏蔽现有项目文件，未读取
  或修改项目正文。
- external MCP turn `019fa22c-e95c-7311-b340-4d40defade51` 连续调用
  `workspace_git_status` 5 次。5/5 均为 `exitCode=0`，实际 cwd 均为
  `/home/unitree/mimiclite-sim2real`；审计 P50 为 81 ms，最大值为 93 ms。
- 五次审计均为 `clientSource=external-mcp`、`target=remote`、
  `rootId=remote-primary`、`rootRole=primary`，没有本地回退。
- 测试 turn 清理 `.git` 指针和隔离 Git 数据目录后，独立 VS Code transport 探针再次
  确认两者均不存在；主根恢复到测试前的非 Git 状态。

## 同 thread 双向观测

- 外部 CLI thread `019fa221-1461-7b43-99f0-5bfe55ad59dc` 的 turn
  `019fa221-14a2-7470-b3c4-bed08a24006e` 启动一次含 10 秒等待的远端命令。
- 审计确认 `remote_exec started` 后，CLI 使用该 turn 的正确 `expectedTurnId`
  发送 steer。响应保持相同 turn ID，最终历史同时包含原消息和 steer 消息，最终回复
  只为 `STEERED_0330`。
- turn 为 `completed`，耗时 19,555 ms。独立 Bridge MCP 随后读取到同一完整历史。
- 目标 turn 的 `turn/started` 和 `turn/completed` 各 1 条、命令输出 delta 2 条、
  agent message delta 6 条；35 条通知被转发到 VS Code，精确 JSON 通知重复帧为 0。

该样本证明 CLI 到共享 thread、VS Code 通知和 MCP 历史读取方向的一致性；官方 UI
主动发起 turn/steer/cancel 及视觉流式状态仍需人工操作。

## 托管 CLI 入口

- `~/.local/bin/codex-vscode` 和 `~/.local/bin/codex` 均为符号链接，目标一致指向
  `0.3.30-8aedab7317ae1dac/codex-bridge-shim.cjs`，证明升级迁移已生效。
- `codex-vscode` 使用当前活动 thread 启动真实官方 TUI。观测到 CLI `0.145.0`，
  `thread/resume` 在 50 ms 内成功；退出时 `thread/unsubscribe` 成功，网关断开审计为
  `succeeded`。版本值只作诊断，不参与准入。
- 使用已经不再是会话活动 thread 的显式参数时，托管入口以
  `No active VS Code Codex thread matches` 失败关闭，没有附着错误会话。
- 当前同时存在 `g1_1` 和另一个不属于本任务的活动 Bridge 会话。普通 `codex` 无参数
  入口因此以 `More than one active VS Code Codex thread matches` 失败关闭，并要求
  使用带 `sessionPid` 的 `codex-vscode`；没有擅自选择会话。单会话下的普通 `codex`
  自动附着仍待后续合适窗口复核。

## 安全复扫

活动 transport、外部网关和上游共 3 个令牌样本只在内存中读取，扫描只输出计数：

| 来源 | 文件或进程数 | 精确令牌命中 | 私钥/Bearer/`sk-` 形式命中 |
| --- | ---: | ---: | ---: |
| Bridge 审计 | 1 | 0 | 0 |
| Code 日志 | 588 | 0 | 0 |
| Git 跟踪文件（排除两个故意构造的测试夹具） | 212 | 0 | 0 |
| 本地进程参数 | 701 | 0 | 0 |
| MCP 配置 | 1 | 0 | 0 |

- 远端隔离测试路径、审批标记进程、Codex/app-server 进程和敏感环境变量键均为 0。
- 当前审计只有 `vscode-remote` 连接模式；OpenSSH 操作数、额外认证和成功本地项目操作
  均为 0。
- 扫描没有输出、记录或提交任何密码、私钥、Token、完整环境变量或 transport 令牌。

## 结论与遗留

- 本轮通过：外部 CLI 审批接受、审批等待中取消、部分补丁原子失败、补丁字节上限、
  补丁执行中断线保持原文件、Core 本地风险命名空间阻断、远端 Git 工具 5/5、同 turn
  steer/通知去重、`codex-vscode` 迁移与附着、当前会话安全扫描。
- 待补测：官方 UI 方向的审批、三次取消、turn/steer/流式视觉和完整历史。
- 待补测：本地次级根授权/撤销、本地写入、真实 Git 根、附件与远程 Diff 视觉。
- 待补测：Executor 独立失联或窗口 transport 断开时的写入矩阵、外部权限撤销、单
  活动会话下普通 `codex` 自动附着。
- 待补测：真实 Remote SSH 窗口关闭；该操作最终统一人工执行。
- Windows x64 与 OpenSSH 回退保持暂停/未选择，不从本轮 Linux 结果推断。
- 最终结论：Linux 可注入矩阵限定范围通过。
