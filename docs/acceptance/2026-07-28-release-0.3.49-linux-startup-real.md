# Bridge `0.3.49` Linux 首次启动链路实机验收

日期：`2026-07-28`

执行人：用户（安装后窗口重载与 Remote SSH 重新连接）、Codex（日志、审计、进程、
真实 turn、远程操作和 MCP 复核）

复核人：Codex

## 变更与范围

| 项目 | 内容 |
| --- | --- |
| Bridge 版本 | `0.3.49` |
| 实现提交 | `b4ba904`、`680ad6c`、`4472c7e` |
| 变更摘要 | 先配置官方扩展 UI 位置再检测运行时；Linux 使用自包含 Shim；`ready` 等待当前 Shim 和 app-server initialize |
| 命中的升级触发项 | 官方扩展位置、窗口重载、Linux Shim、app-server、状态机、Diagnostics、Remote SSH、MCP 路由 |
| 声明范围 | Linux x64 Controller 到 `g1_1` Linux arm64 Remote SSH 的精确候选实机；Windows x64 不外推 |

## 兼容集合

| 组件 | 实测版本 | 证据 |
| --- | --- | --- |
| VS Code | `1.130.0`，Linux x64 | 本机 CLI 和活动窗口进程 |
| Remote SSH | `0.124.0` | 本机扩展列表 |
| 官方 Codex 扩展 | `26.721.41059` | 本地 UI Extension Host 和活动 Codex 进程 |
| 官方扩展内置 Codex/app-server | `0.146.0-alpha.3.1` | 新建 thread 元数据和活动 app-server |
| Bridge Controller / Shim | `0.3.49` | 已安装扩展和内容寻址 Shim 路径 |
| Remote Executor | `0.2.19` | 实现与协议未变；真实远端命令和 MCP relay 通过 |
| Remote SSH 目标 | `g1_1`，Linux arm64 `5.15.148-tegra` | 活动 VS Code Remote transport |
| 远端主根 | `/home/unitree/mimiclite-sim2real` | 会话、审计、命令结果和 Codegraph 进程 |

版本值只作为诊断和回归证据，不作为运行时准入门禁。

## 启动与状态

- 精确 `codex-remote-bridge-0.3.49-linux-x64.vsix` 已使用强制安装。首次激活把托管入口
  迁移到
  `0.3.49-419bd76080166a6a/codex-bridge-shim`，随后记录
  `window reload request canceled this Extension Host activation as expected`，没有再把
  正常窗口重载显示为 settings bootstrap 失败。
- 重载后的官方 `openai.chatgpt` 位于本地 UI Extension Host PID `145866`。当前
  Shim PID `146078` 是本地 ELF 自包含入口，其子进程 PID `146370` 是官方扩展内置
  Codex；远端 Extension Host 不运行官方 Codex。
- 已安装 Shim 为 x86-64 ELF、模式 `0755`、`125,242,560` 字节，SHA-256 为
  `419bd76080166a6ae74ce0fd7889eaaa2eaec808d3bfc7e328396a510922097a`。
  运行状态中的 `nodeExecutable=null`，启动命令不经过 `/usr/bin/env node`。
- Bridge 审计在 `08:12:27.264Z` 进入 `connecting`，在 `08:12:30.629Z` 因
  app-server 尚未初始化进入 `degraded`，在 `08:12:30.879Z` 收到当前 Shim 心跳后
  进入 `ready`。`connecting -> ready` 为 `3,615 ms`，等待态持续 `250 ms`，没有仅凭
  Remote Executor 探针提前报告完整就绪。
- Shim 在 `1785312750532 ms` 启动，VS Code app-server initialize 在
  `1785312750659 ms` 完成，间隔 `127 ms`。状态文件同时确认
  `running=true`、`shimLastExitCode=null`、`appServerLastError=null`；对应
  Diagnostics 投影为 `shimStarted=true`、`appServerInitialized=true`。
- 重载后的官方 Codex 日志包含 app-server 监听和两个 initialize 请求；没有
  `/usr/bin/env: node`、退出 `127`、`stdin is destroyed`、`Codex process is not
  available` 或 fatal process error。官方扩展仍会尝试从本机监视远端 POSIX 路径并
  记录既有 `git-init-watcher ENOENT`，本目标没有伪造或改写 workspace URI 来隐藏它。
- 当前工作区的历史会话描述符中只有 Extension Host PID `145866` 仍存活；进程树中该
  窗口只有一个 `0.3.49` Shim。其他可见旧版 Shim 属于同时打开的其他 VS Code 窗口，
  不属于本次 `g1_1` 会话，也未被误杀。

## 真实工具回合

| 项目 | 结果 |
| --- | --- |
| Bridge 会话 | PID `146078`，host `g1_1`，workspace `/home/unitree/mimiclite-sim2real` |
| thread | `019facf1-1cc3-7882-8585-3162cc995a3d` |
| turn | `019facf1-1d05-7730-91b6-b5fa4d054951`，`13,935 ms` 完成 |
| 远端命令 | 模型直接调用 `remote_exec({"argv":["pwd"]})`，省略 `target/rootId` |
| 命令结果 | `rootId=remote-primary`、`target=remote`、退出码 `0`、实际执行 `19 ms`、stdout 为远端主根 |
| MCP 工具 | 直接调用 `server=codegraph` / `tool=codegraph_status`，状态 `completed`，`29 ms` |
| Codegraph 结果 | 远端项目索引 `93` 个文件、`1,758` 个节点、`3,596` 条边 |
| 远端进程 | `g1_1` 上的 Codegraph MCP 进程带 `serve --mcp --path /home/unitree/mimiclite-sim2real` |
| 禁止路径 | 没有通过 `remote_exec` 调 Codegraph CLI，没有因 MCP Schema 缺少 `target/rootId` 拒绝 |
| 模型终态 | `REMOTE_STARTUP_0349_OK` |

Bridge 审计按顺序记录 `thread/start`、`turn/start`、自动 `full-access` 审批和唯一一次
`target=remote` 的 `remote_exec`；rollout 中保留远端 `rootPath`、`actualCwd`、stdout
及直接 Codegraph MCP 工具项。任务成功到达当前 Shim，并沿活动 VS Code Remote SSH
transport 落在规范远端主根。

## 自动化与产物

| 检查 | 结果 |
| --- | --- |
| 定向自动化 | 自动初始化顺序 11 项；运行状态、SharedAppServer 和 Controller 状态 21 项通过 |
| 完整 `npm run check` | 62 个测试文件通过、1 个真实远端条件文件跳过；290 项通过、6 项跳过、0 失败 |
| 类型检查、构建、Shim 冒烟、构包 | 全部通过 |
| Linux x64 VSIX | `44,610,418` 字节，SHA-256 `1f4f8829c4b098f5e0c7ee6080da8bd9ff11a5e3a4b1b3a36ce93a93d07bc876` |
| Linux x64 Shim | `125,242,560` 字节，SHA-256 `419bd76080166a6ae74ce0fd7889eaaa2eaec808d3bfc7e328396a510922097a` |
| Executor 两份 VSIX | 各 `69,139` 字节，SHA-256 `5dd9fc23f92ada2e69910da94bc864afd00b6552cd5122477495a66d5f8ed188` |

## 门禁与结论

| 门禁 | 状态 | 说明 |
| --- | --- | --- |
| G0 版本与协议 | 通过 | Controller/Shim 为精确 `0.3.49`；Executor 实现与协议未变 |
| G1 自动化 | 通过 | 完整检查 0 失败 |
| G2 产物 | Linux 通过 | 已安装并运行精确 Linux x64 自包含产物；Windows 待原生验证 |
| G3 官方界面/App Server | 通过（本目标） | Webview app-server initialize、真实新 thread 和 turn 完成 |
| G4 Remote SSH 执行 | 通过（本目标） | `pwd` 省略根参数后仍准确落在远端主根 |
| G5 stdio MCP | 通过 | Codegraph MCP 直接调用并由远端进程确认 |
| G6 生命周期 | 通过（本目标） | reload cancellation、等待态、心跳转 ready 和当前进程归属通过 |
| G7 安全失败 | 通过 | 无 initialize 不进入 ready；状态与记录不包含凭据 |
| G8 双平台 | 待补测 | Windows x64 原生构建和实机仍未执行 |
| G9 证据闭环 | 通过（本目标） | 版本、日志、审计、状态、进程、真实 turn、量化值和产物均已记录 |

结论：`0.3.49` 已在真实 Linux x64 Remote SSH 窗口关闭本次反馈的三个启动链路问题：
官方扩展检测不再发生在 UI 位置配置之前，Linux Shim 不依赖 PATH 中的 Node，Bridge
只在当前 Shim 存活且官方 app-server 完成 initialize 后显示 `ready`。官方 Codex
面板完成真实远端命令与远程 Codegraph MCP 调用。本目标 Linux 链路闭环；Windows x64
继续保持待补测，不扩大双平台支持声明。
