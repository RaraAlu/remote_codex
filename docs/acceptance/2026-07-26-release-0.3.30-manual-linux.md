# Bridge `0.3.30` Linux 当前候选人工验收记录

日期：2026-07-26

执行人：用户（Remote SSH 窗口关闭、重开、重载及官方 UI 操作）；Codex（日志、审计、
会话、进程与量化复核）

复核人：待补测（L02-L07 及 L01 最后一次带载冷启动）

## 范围与边界

本记录承接
`docs/acceptance/2026-07-26-release-0.3.30-injectable-matrix.md`，只记录当前
`0.3.30` Linux 候选的人工窗口和官方 UI 样本，不复用 `0.3.26` 的启动或任务结果。

- Controller/Shim 为 `0.3.30`，Shim 内容目录为
  `0.3.30-8aedab7317ae1dac`；Executor 为 `0.2.16` / 协议 9。
- 官方扩展为 `openai.chatgpt@26.721.41059`，内置 Codex/app-server 为
  `0.146.0-alpha.3.1`，VS Code 为 `1.130.0`。
- 目标为 Remote SSH 主机 `g1_1`，规范工作区为
  `/home/unitree/mimiclite-sim2real`。
- 全程复用 VS Code Remote transport，没有启动 OpenSSH 回退或第二条认证链路。
- Windows x64 继续暂停，不从本记录推断其运行时结果。

## 当前候选启动

### 热启动

| 样本 | `configuring` | `ready` | 耗时 | 结果 |
| --- | --- | --- | ---: | --- |
| 1 | `05:38:39.930Z` | `05:38:43.678Z` | 3,748 ms | 通过 |
| 2 | `06:13:33.823Z` | `06:13:37.532Z` | 3,709 ms | 通过 |
| 3 | `06:18:12.730Z` | `06:18:16.337Z` | 3,607 ms | 通过 |

- 当前候选热启动为 3/3，P50 为 3,709 ms，最大值为 3,748 ms，失败数为 0。
- 上一同平台记录的热启动 P50 为 3,491 ms；当前 P50 未同时超过其 1.5 倍和
  `+5 秒`，不构成量化回归。
- 每次均恢复到 `g1_1` 和规范工作区；旧 Extension Host、Shim/app-server 和会话文件
  均退出或移除。

### 冷启动

| 样本 | `configuring` | `ready` | 耗时 | 结果 |
| --- | --- | --- | ---: | --- |
| 1 | `06:19:08.877Z` | `06:19:12.545Z` | 3,668 ms | 通过 |
| 2 | `06:19:55.007Z` | `06:19:58.904Z` | 3,897 ms | 通过 |
| 3 | 待补测 | 待补测 | 待补测 | 与 L07 带载窗口关闭合并 |

- 两次已执行样本均为完整关闭并重新打开 Remote SSH 窗口；对应旧 Extension Host、
  Shim/app-server、Codex 进程和会话文件均已退出或移除。
- 当前候选冷启动仅为 2/3，不能计算最终 P50 或宣称最低样本完成。第三次按执行计划与
  L07 的带载窗口关闭合并，避免重复关闭窗口。

## 官方 UI 直接新建

官方面板直接新建三个独立 thread。任务创建耗时取 Codex 日志中紧邻目标
`Conversation created` 之前的 `thread-start config resolved` 到创建成功的差值；
完整 turn 耗时另列，不混入创建指标。

| 样本 | Thread | 创建耗时 | 完整 turn | 结果 |
| --- | --- | ---: | ---: | --- |
| 1 | `019fa23b-a92c-7b20-b715-e5fbc339353e` | 1,601 ms | 7,796 ms | `OFFICIAL_NEW_1_OK` |
| 2 | `019fa23d-4781-7482-9487-6814e47a377b` | 75 ms | 6,263 ms | `OFFICIAL_NEW_2_OK` |
| 3 | `019fa240-8e3a-7a33-9644-966c7502d77c` | 82 ms | 8,228 ms | `OFFICIAL_NEW_3_OK` |

- 官方任务创建为 3/3，P50 为 82 ms，最大值为 1,601 ms，失败数为 0。
- 上一份同平台记录只有一个 74 ms 官方 UI 直接新建参考样本，不能用于宣称历史性能
  稳定；当前 P50 未命中量化回归条件。
- 三个 thread 均由当前 `g1_1` Bridge 会话读取为 `idle`，工作区根均为
  `/home/unitree/mimiclite-sim2real`；Codex 日志中的 `Unknown local project`
  命中为 0。
- 三个 turn 均为 `completed`，回复与预期完全一致，item 中没有工具调用。

## 官方 UI 直接恢复

用户从官方历史记录分别打开上述三个 thread，再在原 thread 中发送一个新 turn。Bridge
会话读取确认每个 thread 均保留原始新建 turn，并新增对应恢复 turn，没有误建新 thread。

| 样本 | Thread | 恢复派发耗时 | 完整 turn | 结果 |
| --- | --- | ---: | ---: | --- |
| 1 | `019fa23b-a92c-7b20-b715-e5fbc339353e` | 21 ms | 2,426 ms | `OFFICIAL_RESUME_1_OK` |
| 2 | `019fa23d-4781-7482-9487-6814e47a377b` | 23 ms | 4,848 ms | `OFFICIAL_RESUME_2_OK` |
| 3 | `019fa240-8e3a-7a33-9644-966c7502d77c` | 24 ms | 4,902 ms | `OFFICIAL_RESUME_3_OK` |

- 恢复派发耗时取 Codex 日志的 turn-start 配置完成到 rollout `task_started` 的差值；
  3/3 成功，P50 为 23 ms，最大值为 24 ms。
- 三个恢复 turn 均为 `completed`，回复与预期完全一致，item 中没有工具调用；完整
  turn 的 P50 为 4,848 ms，最大值为 4,902 ms。

## Shim、审计与已知观察

- 官方任务批次期间没有新的 `shim.start`，当前 Extension Host、Shim/app-server 和
  上游 Codex 进程持续存活；新建和恢复均进入冷启动样本 2 建立的同一当前 Shim。
- 从官方任务批次开始到复核时，Bridge 审计中的成功本地项目操作为 0，Bridge 状态失败
  为 0。官方任务没有请求工具，因此本记录不以其替代 M02-M05 的远端工具和视觉矩阵。
- 官方扩展日志持续出现 `git-init-watcher` 对远端 POSIX 根执行本地文件监视而得到
  `ENOENT` 的警告。该警告没有造成任务失败、`Unknown local project` 或成功本地项目
  操作，但保留到 L02/L03 的授权根、附件、本地诱饵和普通本地窗口矩阵继续观察。
- 日志中还有官方 MCP 资源枚举不支持、设置接口 403 和状态数据库回退警告；本批次
  thread 创建、恢复、流式完成和历史读取均成功，因此只作为观察项记录，不改写为零或
  隐去。

## 当前结论

- L01 热启动最低样本已完成：3/3。
- L01 官方面板直接新建和直接恢复最低样本已完成：各 3/3。
- L01 冷启动已完成 2/3；最后一次与 L07 带载窗口关闭合并，L01 总体仍为 `待补测`。
- L02-L07 尚未完成；Linux 最终限定声明仍不能形成。
